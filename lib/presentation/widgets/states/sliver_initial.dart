import 'package:flutter/material.dart';

class SliverInitial extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SliverToBoxAdapter(
      child: Container(),
    );
  }
}
